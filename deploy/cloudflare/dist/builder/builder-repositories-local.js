/**
 * Builder B3 — local repository implementations (stub).
 * Future: project/partner/thread repos backed by localStorage.
 */
(function (global) {
  "use strict";

  global.TasuBuilderRepositoriesLocal = global.TasuBuilderRepositoriesLocal || {};
})(typeof window !== "undefined" ? window : globalThis);
