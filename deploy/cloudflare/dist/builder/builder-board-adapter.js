/**
 * Builder B3 — board feed adapter (stub).
 * Future: listBoardProjects, commitBoardMutation, applyToProject, ensureFeedListings.
 * Stub: no methods — builder.js / TasuBuilderBoardFeed fallbacks remain primary.
 */
(function (global) {
  "use strict";

  global.TasuBuilderBoardAdapter = global.TasuBuilderBoardAdapter || {};
})(typeof window !== "undefined" ? window : globalThis);
