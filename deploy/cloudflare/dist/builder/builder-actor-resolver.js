/**
 * Builder B3 — actor resolver (stub).
 * Future: bridge TasuBuilderActorIdentity with B3 session/repos.
 * MVP: builder-actor-identity.js remains authoritative.
 */
(function (global) {
  "use strict";

  global.TasuBuilderActorResolver = global.TasuBuilderActorResolver || {};
})(typeof window !== "undefined" ? window : globalThis);
