/**
 * Builder B3 — Supabase repository implementations (stub).
 * Future: sql/builder-schema.sql backed repos. No live connection in stub.
 */
(function (global) {
  "use strict";

  global.TasuBuilderRepositoriesSupabase = global.TasuBuilderRepositoriesSupabase || {
    isEnabled() {
      return false;
    },
  };
})(typeof window !== "undefined" ? window : globalThis);
