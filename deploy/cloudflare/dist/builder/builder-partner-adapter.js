/**
 * Builder B3 — partner/application adapter (stub).
 * Future: resolvePartnerForApplication, upsertApplication, recordPartnerEvent.
 * Stub: no methods — builder.js optional hooks fall back to in-memory MVP state.
 */
(function (global) {
  "use strict";

  global.TasuBuilderPartnerAdapter = global.TasuBuilderPartnerAdapter || {};
})(typeof window !== "undefined" ? window : globalThis);
