/**
 * Builder B3 — session state (stub).
 * Future: MVP role/partner_id session wrapper.
 */
(function (global) {
  "use strict";

  global.TasuBuilderSession = global.TasuBuilderSession || {};
})(typeof window !== "undefined" ? window : globalThis);
