/**
 * Builder B3 — shared repository base (stub).
 * Future: common CRUD helpers for B3 entities.
 */
(function (global) {
  "use strict";

  global.TasuBuilderRepository = global.TasuBuilderRepository || {};
})(typeof window !== "undefined" ? window : globalThis);
