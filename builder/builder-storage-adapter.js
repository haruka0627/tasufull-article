/**
 * Builder B3 — file/photo storage adapter (stub).
 * Future: uploadProjectPhoto, normalizeChatAttachments, listFiles.
 * Stub: no methods — builder.js keeps thread/siteData local paths.
 */
(function (global) {
  "use strict";

  global.TasuBuilderStorageAdapter = global.TasuBuilderStorageAdapter || {};
})(typeof window !== "undefined" ? window : globalThis);
