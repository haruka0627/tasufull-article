/**
 * Builder B3 — PDF output adapter (stub).
 * Future: listThreadPdfOutputs, saveThreadPdfOutput.
 * Stub: no methods — builder.js uses thread.pdf_outputs fallback.
 */
(function (global) {
  "use strict";

  global.TasuBuilderPdfAdapter = global.TasuBuilderPdfAdapter || {};
})(typeof window !== "undefined" ? window : globalThis);
