/**
 * Builder B3 — notification adapter (stub).
 * Future: countUnread, markAsRead, markAllAsRead, persistFromMvp.
 * Stub: no methods — builder.js uses tasful:builder:mvp:notifications:v1.
 */
(function (global) {
  "use strict";

  global.TasuBuilderNotificationAdapter = global.TasuBuilderNotificationAdapter || {};
})(typeof window !== "undefined" ? window : globalThis);
