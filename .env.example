# ローカル開発用環境変数（コピーして .env を作成 · Git に含めない）
# wrangler pages dev は npm run dev 経由で --env-file=.env を読み込む

# AI 運営秘書 — DeepSeek（サーバー側のみ · クライアントへ渡さない）
DEEPSEEK_API_KEY=
# DEEPSEEK_CHAT_MODEL=deepseek-chat

# Supabase（ビルド / 検証スクリプト用 · 既存）
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
