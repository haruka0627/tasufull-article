/**
 * TasuFull — ワーカー一覧カード サンプルデータ
 */
/** index.html にワーカーカードを静的配置するため、自動挿入用サンプルは空 */
const WORKER_CARD_SAMPLES = [];

if (typeof window !== "undefined") {
  window.WORKER_CARD_SAMPLES = WORKER_CARD_SAMPLES;
}
